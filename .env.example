DATABASE_URL="postgresql://user:password@host:5432/equiprent?sslmode=require"
JWT_SECRET="change-this-to-a-random-secret-string"

# Vercel: add both in Project → Settings → Environment Variables (all environments + build).
